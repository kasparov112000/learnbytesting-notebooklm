apiVersion: v1
kind: Service
metadata:
  name: {{ include "notebooklm.fullname" . }}
  labels:
    {{- include "notebooklm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "notebooklm.selectorLabels" . | nindent 4 }}
